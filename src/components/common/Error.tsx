import { FC } from "react";

const Error: FC = () => (
  <div className="my-10 text-center text-red-500 mx-4">
    Please make sure the backend server is running correctly on port 3000 .
  </div>
);

export default Error;
